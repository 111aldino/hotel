

const UpdateRoomPage = async ({
  params

}: {

  params: Promise<{ id: string }>

}) => {

    const roomId = (await params).id
    if(!roomId) return not

  return(
      <div className=""></div>
  ) 
}

export default UpdateRoomPage;
